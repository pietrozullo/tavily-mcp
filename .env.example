# Required — get one at https://tavily.com
TAVILY_API_KEY=

# Optional — override the default (3000)
# PORT=3000

# Optional — full public URL of this MCP server (used for OAuth metadata etc.)
# MCP_URL=http://localhost:3000
